# Invoice Processing RPA

## Overview
This script extracts invoice details from PDFs and saves them in a structured CSV format.

## Installation
1. Install dependencies:
   ```sh
   pip install pdfplumber

