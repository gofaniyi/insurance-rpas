# Data Scraping RPA

## Overview
This RPA script scrapes product information from a given webpage and saves it as a CSV file.

## Installation
1. Install dependencies:
   ```sh
   pip install requests beautifulsoup4
